[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "incopilot"
version = "0.1.0"
description = "AI Incident Copilot CLI"
requires-python = ">=3.10"
dependencies = ["rich==13.9.4"]

[project.scripts]
incopilot = "incopilot.cli:main"
